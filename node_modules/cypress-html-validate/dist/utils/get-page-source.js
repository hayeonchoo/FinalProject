"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPageSource = void 0;
function getPageSource(document) {
    const matchCypress = new RegExp('<script type="text/javascript">.*?var Cypress = window.Cypress.*?</script>');
    const root = document.documentElement;
    const source = root.outerHTML.replace(matchCypress, (match) => {
        /* force disable rules for the cypress injected script tag */
        return `<!-- [html-validate-disable-next require-sri, script-type] -->${match}`;
    });
    return source;
}
exports.getPageSource = getPageSource;
